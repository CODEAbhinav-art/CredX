import AppShell from "@/components/layout/AppShell";

export const metadata = {
  title: "Dashboard | CredX",
  description: "Your alternative credit dashboard.",
};

export default function DashboardPage() {
  return <AppShell />;
}
